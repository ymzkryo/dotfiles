# カレンダー同期設定ファイル
#
# このファイルをコピーして設定してください:
#   cp sync_calendar.conf.example ~/dotfiles/private/.config/sync_calendar/sync_calendar.conf
#   ln -sf ~/dotfiles/private/.config/sync_calendar/sync_calendar.conf ~/.config/sync_calendar.conf

# ===== 同期元カレンダー =====
# 方法1: 単一カレンダー（シンプル）
# WORK_CALENDAR="work@company.com"

# 方法2: 複数カレンダー（配列形式）
# 形式: "カレンダー名:プレフィックス:逆方向同期フラグ"
#   - プレフィックス: 同期先のタイトルに [プレフィックス] として付与
#   - 逆方向同期フラグ: true のカレンダーに --reverse で個人予定を同期
WORK_CALENDARS=(
    "work@company.com:work:true"      # → [work] 会議タイトル、逆方向同期先
    # "project@example.com:project"   # → [project] 会議タイトル（フラグ省略可）
    # "team@company.com:team:false"   # → [team] 会議タイトル
)

# ===== 同期先カレンダー =====
PERSONAL_CALENDAR="personal@gmail.com"

# ===== オプション =====
# 何日先まで同期するか（デフォルト: 7）
DAYS_AHEAD=7

# ===== 逆方向同期（個人→会社） =====
# WORK_CALENDARSで :true を指定したカレンダーに同期される

# 同期時のタイトル（デフォルト: "予定あり"）
# REVERSE_SYNC_TITLE="予定あり"

# 同期対象のプレフィックス（省略時はプレフィックスなしの予定が対象）
# 例: [private] や [block] がついた予定だけを同期
# REVERSE_SYNC_PREFIXES=("private" "block")
#
# 単一の場合は以下も可（後方互換性）
# REVERSE_SYNC_PREFIX="private"

# 後方互換性: WORK_CALENDARSで指定しない場合は以下も使用可
# REVERSE_SYNC_TARGET="work@company.com"
